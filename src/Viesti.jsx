import React from 'react';

const Viesti = (props) => {
        return (
                <div>
                <p>{props.teksti}</p>
                <p>{props.teksti_2}</p>
                </div>
        );
};

export default Viesti;